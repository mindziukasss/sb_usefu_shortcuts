<%= form_for @textsublime do |f| %>
	<% if @textsublime.errors.any? %>
		<h2><%= pluralize(@textsublime.errors.count, "error") %> prevented this text  from saving:</h2>
		<ul>
			<% @textsublime.errors.full_text.each do |txt| %>
				<li><%= txt %></li>
			<% end %>
		</ul>
	<% end %>

	<%= f.label :title, "Combination" %><br>
	<%= f.text_field :title %>

	<br>
	<br>

	<%= f.label :description, "Write function" %><br>
	<%= f.text_area :description %>

	<br>
	<br>

	<%= f.submit "Save", class: "btn btn-default"%>

<% end %>

